fastapi==0.115.6
uvicorn[standard]==0.32.1
motor==3.6.0
pymongo==4.9.2
qdrant-client==1.12.1
upstash-redis==1.2.0
groq==0.13.1
pydantic==2.10.3
pydantic-settings==2.7.0
python-dotenv==1.0.1
httpx==0.27.2

# Embeddings (Phase 3) — ONNX Runtime via fastembed, not PyTorch. Same model
# weights (sentence-transformers/all-MiniLM-L6-v2, verified bit-for-bit
# compatible output), a fraction of the RAM: torch's own baseline overhead
# alone was blowing past Render's 512MB free-tier cap before a single model
# was even loaded.
fastembed==0.8.0
