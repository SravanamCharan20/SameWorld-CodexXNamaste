fastapi==0.115.6
uvicorn[standard]==0.32.1
motor==3.6.0
pymongo==4.9.2
qdrant-client==1.12.1
upstash-redis==1.2.0
groq==0.13.1
pydantic==2.10.3
pydantic-settings==2.7.0
python-dotenv==1.0.1
httpx==0.27.2

# Embeddings (Phase 3). torch is installed separately in the Dockerfile from
# the CPU-only wheel index — the default PyPI wheel bundles CUDA and is
# several times larger for zero benefit on a CPU-only deploy target.
sentence-transformers==5.6.0
